export interface LoginObjectType {
    email: string;
    password: string;
}

export interface MessageObjectType {
    id: string;
    email: string;
    name: string;
    message: string;
    time: string;
}